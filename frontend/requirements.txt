# ⚠️ ESTE ARQUIVO É PARA A PASTA frontend/ — não confundir com o requirements.txt do backend!
#
# Usamos ">=" em vez de "==" de propósito: o Render roda Python 3.14 (bem
# recente), e versões antigas travadas (ex: pandas==2.0.0) não têm pacote
# pronto (wheel) pra essa versão do Python — o pip tenta compilar do zero
# e quebra por falta do pkg_resources. Com ">=", o pip pega a versão mais
# nova compatível, que já vem pronta pra instalar.
streamlit>=1.38
pandas>=2.2
requests>=2.32
plotly>=5.24
openpyxl>=3.1
xlsxwriter>=3.2
reportlab>=4.2